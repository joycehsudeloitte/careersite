<section class="ldc-mentor-section">
  <div class="mentor-container">

    <!-- Person 1 -->
    <div class="mentor-card fade-element">
      <div class="mentor-photo">
        <img src="https://raw.githubusercontent.com/joycehsudeloitte/careersite/main/images/Profile/Frank%20Hsueh.jpg" alt="Frank Hsueh">
      </div>

      <div class="mentor-text-box">
        <div class="quote-bar"></div>

        <p class="mentor-quote">
          “在 LDC，我最喜歡的是團隊合作的文化。我們不只是完成任務，
          而是一起思考、一起成長，並為審計帶來真正的價值。”
        </p>

        <p class="mentor-name">Frank Hsueh｜LDC Leader</p>
      </div>
    </div>

    <!-- Person 2 -->
    <div class="mentor-card fade-element">
      <div class="mentor-photo">
        <img src="https://raw.githubusercontent.com/joycehsudeloitte/careersite/main/images/Profile/Allen%20M.%20Chiang.jpg" alt="Allen M. Chiang">
      </div>

      <div class="mentor-text-box">
        <div class="quote-bar"></div>

        <p class="mentor-quote">
          “加入 LDC 後，我學到最多的是如何在壓力下保持品質，
          並與團隊一起找到最有效率的解決方案。”
        </p>

        <p class="mentor-name">Allen M. Chiang｜Partner</p>
      </div>
    </div>

  </div>
</section>

<style>
.ldc-mentor-section {
  width: 100%;
  background-color: #ffffff;
  padding: 60px 20px;
  display: flex;
  justify-content: center;
}

.mentor-container {
  max-width: 1200px;
  width: 100%;
  display: flex;
  gap: 40px;
  align-items: stretch;
  justify-content: space-between;
}

/* ===== 卡片（左右兩張） ===== */
.mentor-card {
  width: 48%;
  background: #ffffff;
  display: flex;
  gap: 20px;
  align-items: flex-start;
  opacity: 0;              /* fade 初始 */
  transform: translateY(30px);
  transition: opacity 1s ease, transform 1s ease;
}

/* 淡入後的 class */
.fade-in {
  opacity: 1;
  transform: translateY(0);
}

/* ===== 圓形頭像 ===== */
.mentor-photo img {
  width: 180px;
  height: 180px;
  border-radius: 50%;     /* 圓形裁切 */
  object-fit: cover;
  object-position: center;
  box-shadow: 0px 8px 16px rgba(0,0,0,0.15);
}

/* ===== 文字區塊 ===== */
.mentor-text-box {
  flex: 1;
  position: relative;
  padding-left: 20px;
}

.quote-bar {
  width: 6px;
  height: 100%;
  background-color: #86BC25;
  position: absolute;
  left: 0;
  top: 0;
  border-radius: 4px;
}

.mentor-quote {
  font-size: 18px;
  line-height: 1.6;
  color: #000000;
  margin-bottom: 20px;
}

.mentor-name {
  font-size: 16px;
  font-weight: bold;
  color: #007CB0;
}

/* ===== RWD ===== */
@media (max-width: 768px) {
  .mentor-container {
    flex-direction: column;
    gap: 40px;
  }

  .mentor-card {
    width: 100%;
    flex-direction: column;
    text-align: center;
  }

  .mentor-photo img {
    width: 200px;
    height: 200px;
    margin: 0 auto;
  }

  .quote-bar {
    display: none;
  }

  .mentor-text-box {
    padding-left: 0;
  }
}
</style>

<script>
// ===== Intersection Observer for fade-in =====
const fadeElements = document.querySelectorAll(".fade-element");

const observer = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add("fade-in");
    }
  });
});

fadeElements.forEach(el => observer.observe(el));
</script>
